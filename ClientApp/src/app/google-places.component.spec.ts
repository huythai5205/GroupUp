import { GooglePlacesComponent } from './google-places.component';

describe('GooglePlacesDirective', () => {
  it('should create an instance', () => {
    const directive = new GooglePlacesComponent();
    expect(directive).toBeTruthy();
  });
});
